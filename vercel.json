{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && git config --global --add safe.directory /vercel/path0/flutter && flutter/bin/flutter config --no-analytics && flutter/bin/flutter pub get && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "echo 'No install needed'"
}
